# Brand assets

`stagium-logo.png` is synchronized from `docs/stagium-logo.png` by `npm run sync:brand` before local development and production builds.

The UI keeps an accessible fallback mark so navigation remains professional if the source logo is not present in a fresh checkout.
